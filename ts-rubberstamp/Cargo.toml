[package]
name = "ts-hook-validator"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
diff = "0.1"
regex = "1.10"
chrono = "0.4"
clap = { version = "4.0", features = ["derive"] }
dirs = "5.0"
